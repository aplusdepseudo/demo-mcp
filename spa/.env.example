# Environment variables for the RFP SPA server
# Copy this file to .env and fill in the values before running.

# ─── Required ───────────────────────────────────────────────────────────────

# Microsoft Foundry project endpoint (found in AI Foundry portal > Project overview)
# Format: https://<ai-services-account>.services.ai.azure.com/api/projects/<project-name>
FOUNDRY_PROJECT_ENDPOINT=https://your-account.services.ai.azure.com/api/projects/your-project

# ─── Optional ───────────────────────────────────────────────────────────────

# URL of the MCP procurement server (default: http://localhost:3000)
MCP_SERVER_URL=http://localhost:3000

# Port for the SPA backend server (default: 3000)
# PORT=3000

# Azure credential env vars (if not using az login or Managed Identity)
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
# AZURE_TENANT_ID=
